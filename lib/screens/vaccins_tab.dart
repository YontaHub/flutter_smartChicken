import 'package:flutter/material.dart';

class VaccinsTab extends StatelessWidget {
  const VaccinsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Planification des Vaccins'),
    );
  }
}