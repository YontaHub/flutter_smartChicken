import 'package:flutter/material.dart';

class TraitementsTab extends StatelessWidget {
  const TraitementsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Planification des Traitements'),
    );
  }
}